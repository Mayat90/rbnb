<div class="row">
  <div class="myflex">
    <div>
      <div class="center fondcard">
        <%= image_tag(current_user.photo, size: "150x150", class: "img-thumbnail") %><br/>
        <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-success" %>
      </div>
      <div class="fondcard">
        <h4>Verifications :</h4>
        <p>Pièce Identité <i class="fa fa-check-circle green" aria-hidden="true"></i></p>
      </div>
    </div>
    <div class="mygrow">
      <div>
        <h2><%= current_user.first_name %> <%= current_user.last_name %></h2>
        <p>Nombre d'enfants : <%= current_user.number_of_kids %></p>
      </div>

      <div>
        <h3>description :</h3>
        <p><%= current_user.description %> </p>
      </div>
    </div>
    <div class="flatlist center">
      Mes adresses :
      <% current_user.user_places.each do |user_place| %>
        <% @user_place = user_place %>
       <% current_user.user_places.each do |user_place| %>
        <% @user_place = user_place %>
        <div class="fondcard">
          <%= render 'shared/flat' %>
      <% end %>
    </div>
  </div>
</div>




